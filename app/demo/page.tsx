import { ReactNode } from 'react';
import Buttons from '../../components/demo/Buttons';

const Page = (): ReactNode => (
  <div>
    <h1>Demo Page</h1>
    <Buttons />
  </div>
);

export default Page;
